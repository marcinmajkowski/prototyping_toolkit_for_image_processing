#ifndef ERODEFILTER_H
#define ERODEFILTER_H

class ErodeFilter : public Filter
{
    Q_OBJECT
public:
    explicit ErodeFilter(QObject *parent = 0);

signals:

public slots:

};

#endif // ERODEFILTER_H
